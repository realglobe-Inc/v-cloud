/**
 * Test for SpotFor.
 * Runs with mocha.
 */
'use strict'

const SpotFor = require('../lib/SpotFor')
const { ok, equal } = require('assert')

describe('spot-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
